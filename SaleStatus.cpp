#include "SaleStatus.h"

SaleStatus::SaleStatus( OrderBookEntry _obe,
                        bool _saleStatus)
                        : obe(_obe),
                        saleStatus(_saleStatus)
{
    
}